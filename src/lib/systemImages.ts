// Example mapping of parking system IDs to image URLs
export const systemImages: Record<string, string> = {
  Aps1: "/image.png", // Replace with real images if available
  Aps2: "/placeholder.svg", // Replace with real images if available
};
